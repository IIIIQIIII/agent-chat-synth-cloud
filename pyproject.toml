[project]
name = "agent-chat-synth-cloud"
version = "1.0.0"
description = "Generate realistic multi-turn coding conversations with LLM role-play"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
]

[project.urls]
Repository = "https://github.com/IIIIQIIII/agent-chat-synth-cloud"
